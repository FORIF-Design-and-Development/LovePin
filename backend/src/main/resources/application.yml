spring:
  application:
    name: lovepin

  datasource:
    # MySQL ?? ?? (DB??: lovepin)
    url: jdbc:mysql://localhost:3306/lovepin?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: '1234'

  jpa:
    hibernate:
      # create: ?? ??? ?? ? ?? ?? (???)
      # update: ?? ??? ?? (??/??? ??)
      ddl-auto: update
    show-sql: true # ???? SQL? ??? ??
    properties:
      hibernate:
        format_sql: true # SQL? ?? ?? ???? ??